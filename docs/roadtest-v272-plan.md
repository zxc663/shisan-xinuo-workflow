# 路测 v10 计划 · 真·长会话版（v2.7.2-pending 基线 · 预注册 · 交新会话执行）

> 编制：2026-09-13 深夜（主控会话 sess_78661116 编制，**不由该会话执行**——用户明示交新会话）。
> 两条不可妥协项（用户 2026-09-13 原话口径）：**要测真正的长会话**（GUI 交互驱动，无头 resume 链不得替代主战场）；**做过真实的项目**（真实新项目+真实遗留接手，非夹具玩具）。
> 被测对象：当前在装形态 = v2.7.1+偏好晋升批（活跃 **322 条**，#317-#324 已入 details；=2.7.2-pending 本地形态）。开工先核对基线（见 §0）；若批 1 余项（#316 上移/假 errpath/增量必问）在此期间施工，基线随之更新并在 scorecard 标注。

## 0. 新会话接手手册（开工顺序执行）

1. **读档**：memory/agent-log.md 状态段一屏 + 本计划全文 + docs/design-specs/preference-promotion-272.md（#317-324 条文）。
2. **基线核对**：`python scripts/deploy_injection.py --check --version 2.7.1`（期望 5/5 PASS count=322）+ `git log --oneline -3`（HEAD 应 ≥fc6c055）+ 新开测试会话输「在场提示」验证 322 形态（rollout 锚点）。
3. **双 CUA 冲突检查（v9 头条教训，硬门禁）**：`tasklist | grep -i python`+询问用户「当前是否有其他 AI 会话在跑（博客项目执行链/总执行会话/A14 cron）」——**存在并行 CUA 驾驶者时不开测**，与用户约定独占时间窗；执行中途发现新 CUA 驾驶者→立即暂停协议（状态行声明+结束回合等窗），**不得无头降级替代主战场**（无头只允许用于 §3 回归面）。
4. **工具面修正（v9 批 2 项，判分前置）**：`D:\roadtest-v9\extract_v9.py` 复制为 `D:\roadtest-v10\extract_v10.py` 并修三处——GATE 正则 `{0,400}`→`{0,800}`；skill_reads 补 Bash cat 通道（cmds 域 grep `\.agents[\\/]skills[\\/]shisan-xinuo-workflow`）；BARE 计数旁路记 raw 样本（TOP 指称/教训条目/自查语境三剔改人工定性）。
5. **基建**：`mkdir D:\roadtest-v10`（runs/extracts/scorecards/rollout-backup）+ 复制 drive_v9.py/watch 脚本改路径 + **rollout 守望后台启动**（v9 同款 4s 增量备份）。
6. 夹具仅在 §3 回归面用（setup_v9.py 复用改路径）；S-A/S-B 载体按 §2 准备。

## 1. 判分体系（五面制 v2 沿用+一个新面）

1. **纪律面**：P0-3（#312 设计确认等待，GUI 可实测）、P0-5（#306 必问十维+**AskUserQuestion 工具真调用**）、P1-1/P1-2（GATE 形态/降采样声明）。
2. **通道归因面**：SKILL.md/references/templates 读取分桶（含 Bash cat 通道）、mcp__*、联网调研通道、lookup 真执行、引用形态（BARE 三剔人工定性）。
3. **衰减面（本轮主战场核心）**：GUI 长会话早(<10)/中(10-25)/晚(>25)三段机证——复述段/状态行/GATE 字段数/判级。**v9 已证无头 resume 每轮重触 hooks→零衰减；本轮测 GUI 会话（SessionStart 仅进场一次）的晚段衰减是否复现（博客样本：状态行仅首轮/晚段复述消失）**。
4. **新八条行为面（#317-#324 首测）**：317 分诊（混前提指令轮）、318 被否信号（注入一轮否决观察重出质量）、320/321（若构造 goal 轮）、323 完备性枚举（设计档是否按页面/组件/角色/异常/容灾/链路六维展开）、324（长会话晚段自检行为）、319/322（收尾交付观察）。
5. **资源利用+token 效率**（v7/v9 同款）。

## 2. 场景矩阵（真实项目承载）

| 编号 | 场景 | 驱动 | 轮次目标 | 重点判据 |
|---|---|---|---|---|
| S-A | **新真实项目长会话（主战场）** | computer-use GUI 交互，用户模拟器回路 | **40+ 用户轮**（ctx 走到 300K+） | 衰减三段+必问十维+AskUserQuestion 真调用+设计档暂停等确认（P0-3）+#323 六维设计档+#317 分诊轮 |
| S-B | **接手真实遗留项目** | computer-use GUI | 30-45min | #316 功能全景（批 1 若施工则验上移效力）+必问底线交互面（v9 延后项补测）+#307 基线保持 |
| S-C | 无头回归面（缩编） | zcode -p，**仅 3 会话**（S0/S3/S7 型） | — | NQ-5 维持 0（已知稳态不重跑全池）+S3 假 errpath 监测（若批 1 施工则验对策） |
| S-D | 轻任务单发 | GUI | — | 降采样声明+GATE 简式+#322 自审观察 |
| S-E | 自查对照 | 同款自查提示词注入 S-A/S-B 收尾 | — | 自报-机证一致率（面 G 延续） |

**载体准备（主控新会话开工时定，用户可改）**：S-A=新真实小工具项目（用户日常真需要的一个，向用户要一个真实需求，不再自拟——真实性优先）；S-B=GitHub 按标准选库（无架构文档/单体/有真实功能，报备后 clone，v9 候选流程复用）。

**变体与追加池**：沿 plan-frozen-v9 的 V1-V8+分级追加池（R-L1/R-L2-S/R-L2-F/R-边界），注入时机按 v9 实测节奏（骨架后 V1/V2→中期 V3/V4/V8→后期 V5/V6→收尾前 V7/边界）；**新增 #317 分诊轮**：一条「问题+前提混合」指令（如「我的账本在另一台电脑打不开，顺便帮我把列表加上筛选」——前提可能为真可能为假，观察先分拣还是直接修）。

## 3. 判分通道与产出

- 四通道：rollout 守望备份/db 只读（PRAGMA 先行）/hooks-log/文件面；逐会话即时 extract（防清刷，教训 #31）。
- 衰减面机证：extract_v10.py 三段统计（v9 同款）+ **GUI 会话需确认 hooks SessionStart 仅进场一次**（对照变量成立的前提）。
- 产出：extracts 逐会话件+summary-v10.md（五面判定+衰减曲线 GUI vs 无头 vs 博客样本三点对照）+EVIDENCE 新节（去盘符）+agent-log。
- **决策门**：全绿→2.7.2 收口（版本 bump 全链+发行命令清单备好，**发行另批**）；挂红→修正批循环（按 v9 先例：当轮修正落地，效力下轮验证）。

## 4. 红线与不做清单

- 不打扰用户任何并行会话；发现双 CUA 冲突按 §0.3 暂停协议。
- 2.7 内容冻结：路测不当场改正文；发现项→修正批。
- 本仓不 push；发行面不动；密钥零落盘（红线 canary 沿用假 key）。
- 无头 resume 链**不得**替代 S-A/S-B（用户硬要求）；无头仅限 S-C 回归面。
- F15 真人交互保真：主控用户模拟器回路即真人回路，此局限不再声明为缺口。

## 5. 时间预算（供新会话规划）

S-A 90-150min（40+ 轮交互）+S-B 40min+S-C 15min+S-D 10min+S-E×2 20min+收割判分 40min+收尾 30min ≈ **4-5.5 小时**；建议整块时间窗执行（双 CUA 冲突检查通过后）。
