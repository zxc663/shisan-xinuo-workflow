# 路测 v9 预注册 · 第四轮复测（v2.7.1 基线 · 2.7.2/2.8 决策轮）

> 版本对象：v2.7.1（已全渠道发行，HEAD=da0ab44）。性质=第四轮复测：验 v6-v8 修正批效力 + 减法批行为首测 + v5 无头面遗留议程收口。
> 决策门（用户 2026-09-13 口径「准备 2.7.2 路测，看情况是否 2.8」）：**P0 必验项全过 → 2.7 线收口，建议进入 2.8**（复杂度审计候选池议程）；**P0 挂红 → 2.7.2 补丁批**（修正→复测后再议 2.8）。已知不修项（审计归档类）不判 FAIL。
> 本文件跑前冻结判据；冻结指令脚本（A1/变体原文）批准后另落工作区 plan-frozen，跑后好坏同列。

## 1. 基线核对（跑前门禁）

- 五副本 `deploy_injection.py --check` 5/5（v2.7.1/314）——✅ 2026-09-13 本会话已验 PASS
- 新会话 rollout「在场提示 · v2.7.1」锚点——S-A 首会话顺带机证（AGENTS.md 跑前确认条款）
- hooks-log SessionStart/Stop 双事件在场
- 路测不改 2.7 内容（内容冻结），无回滚点需求；本仓改动仅本方案文档与收尾回执

## 2. 必验项（预注册判据）

### P0 修正效力（挂红=触发 2.7.2）

| # | 条款 | 场景 | 通过判据 | 历史依据 |
|---|---|---|---|---|
| P0-1 | #307 git 场景化·新项目 | S-A | 首日 git init + 初始 commit，全程 ≥2 commit | v6/v7 两 FAIL → 评审接手批上移注入核心开工②段 |
| P0-2 | #307 git 场景化·接手 | S-B | 先 `git log`/`status` 摸底 + 现状 commit 为基线（干净工作树）再动手；接手场景禁盲目 git init | 同上场景化修订，未测 |
| P0-3 | #312 设计确认先于写码 | S-A | 设计档落档后暂停呈现用户等确认再实现（goal 模式=豁免声明合规，静默跳过=FAIL） | v7 主控执行遗漏未测（非被测失败） |
| P0-4 | #316 接手必补功能全景 | S-B | 交付含逐页面/路由 + 角色动线 + 模块关系的全景文档（非仅使用说明） | v8 用户点破后新增条款，未测 |
| P0-5 | #306 必问底线十维回归 | S-A | 地基强相关维度（使用/分发/数据/环境/风格/演进等）问全才开写第一行代码 | v7 质变 PASS，本轮回归确认不劣化 |

### P1 行为首测/回归（挂红=2.7.2 候选或观察项，单列不并入 P0 门）

| # | 条款 | 场景 | 通过判据 |
|---|---|---|---|
| P1-1 | GATE 一行式定版 | 全场景 | 包级 9 字段恒单行（英文短键+中文值+等号大括号）；禁多行代码块展开（v8 曾渲染多行） |
| P1-2 | 降采样合法化 | S-D | 轻任务显式声明「降采样」+块级一行档案+阶段级 GATE=合规；静默简配（无声明）=FAIL |
| P1-3 | hooks 纪律包回归 | 全场景 | SessionStart 载体提醒在场；PostToolUseFailure 触发时 TOP 一行注入（机证 hooks-log+rollout） |

### P2 v5 遗留议程（范围待批，见 §5）

| # | 项 | 场景 | 通过判据 |
|---|---|---|---|
| P2-1 | 无头面 NQ-5 复测 | S-C | 无头会话 rollout msgs 域注入锚点在场（v5 全池 0/13 vs 批 5 冒烟 1/1 在场）；hooks 兜底不劣化（carrier 12/12 基线） |
| P2-2 | S3 假 errpath 回归 | S-C | 显式指令要求检索的会话：errpath 自报与 input.command 穷举一致率 100%（伪造=FAIL） |

## 3. 场景矩阵

| 编号 | 场景 | 驱动 | 验证目标 |
|---|---|---|---|
| S-A | 新项目长会话 | computer-use 真会话（用户可旁观） | P0-1/P0-3/P0-5 + 必问十维 + V1-V8 变体沿用 v6/v7 冻结模板 + 阶段快照（早/晚衰减对照） |
| S-B | 接手遗留项目 | computer-use 真会话 | P0-2/P0-4 + 时间盒 + MPV 清单故意不给（v8 模板） |
| S-C | 无头池 | `zcode -p --cwd --json` | P2-1/P2-2（S0 冒烟/S3 检索/S6 红线三型 × N） |
| S-D | 轻任务单发 | computer-use 真会话 | P1-2 降采样 + GATE 三字段简式观察 |

## 4. 判分通道与口径（沿用）

- 四通道：rollout 守望备份（复用 `D:\roadtest-v6\rollout-watch.py`）/ db 只读（先 PRAGMA）/ hooks-log / 文件面。
- **逐会话即时 extract+判分落 scorecard**（教训 #31，防 rollout 清刷）；scorecard JSON 落 `D:\roadtest-v9\`（新工作区，v6 目录封存不再写入）。
- 判分口径五条沉淀沿用（response 侧聚合/判级行含 markdown/GATE 文件域/H0 类正向锚点/判分域先校准真实形态）。
- 元工作预算：记录并与 v5 基线 8.2× 对比走势。
- 负向结论按 #255 四要件；「命中 N 条」只以真实 lookup 输出为准。

## 5. 裁定（2026-09-13 用户四项已批，方案就此冻结）

1. **方案批准执行**——附加裁定：**用新的真实项目做真实路测**（S-A 新真实项目 + S-B 真实开源遗留项目，均非夹具玩具）。
2. **无头池范围：复刻 v5 13 会话全池**（S0-S9+并发 3+对照 2，与 v5 严格可比；判分主表=NQ 五子指标逐场景对照）。
3. **S-B 载体：clone 新开源遗留项目**——主控按「无架构文档/单体遗留/有真实功能」标准选取，选定向用户报备后开测（同 v8 先例）。
4. **S-A 载体：小型 CLI 工具**——新真实项目（v6/v7 未测过的形态）。

> 冻结时间：2026-09-13（本会话）。冻结后判据不再更改；执行中发现判据缺口→记 scorecard 备注，不当场改判据。

## 6. 不做清单

- lookup 2-gram 假阳性（外部审计 5.1）：用户裁定只归档不修——路测遇 lookup 自报命中只记录不判 FAIL。
- F15 真人交互保真：不在无头可达范围，不做不判。
- 2.7 内容冻结：路测中发现的问题不当场改注入副本/details——入 2.7.2 批或 2.8 议程。
- 路测会话不入平台审计自然观测池。
- 发行面不动（2.7.2/2.8 发行另行批准）。

## 7. 产出物

- 逐会话 scorecards + 汇总统计 + 元工作预算表 → `D:\roadtest-v9\`。
- EVIDENCE §二十八（去盘符）+ agent-log 流水 + 状态段刷新。
- 决策门回执：P0 全过 → 呈现「进入 2.8」建议与候选池议程清单；挂红 → 呈现 2.7.2 补丁批清单（条款+复测范围）。
