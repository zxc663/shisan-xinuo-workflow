# 路测 v12 计划 · 思考链/前置族触达消费无头取证（预注册 · 2026-09-16）

> **执行状态（2026-09-16 06:1x 追加）：三会话已跑完并判分，总结=D:/roadtest-v12/scorecards/summary-v12.md。头条：隐式 C1 思考链零消费（缺口第 2 独立样本）vs 显式 C2 四标记全中 real——能力在场缺自动触发；方法论三发现=rollout messages 层移（3.12.1）/清刷提速分钟级/无头面注入槽位=AGENTS.md 块+hooks 双通道（NQ-5 口径按面分治）。**
> 编制：2026-09-16 05:5x（主控会话，用户指令「无头会话路测补充缺口，自主推进，不允许调用真实电脑操作 MCP」）。
> 基线：v2.9.0（7b0699f+3f36fef+42bd82c+2b546c8，五副本 count=343 5/5）；注入副本=v2.9.0（04:25:06 快照）。
> 模型：GLM-5.3-Flash（平台默认，与 v11 S-D′ 同面）；驱动=ZCode 无头 CLI（`node zcode.cjs -p <prompt> --cwd <fixture> --json`），**全程不调用 computer-use/browser360/playwright 等真实电脑操作 MCP（用户硬约束）**。

## 0. 本轮核心问题（承接 docs/audit-cot-touch-20260916.md 缺口，修复路径 D 的量化样本）

1. **思考链族（rules #7/#9/#10/#11）无头会话是否被显式消费**？——主会话 N=1 零消费，本轮补无头面样本（预期：否；只记描述性证据，N 小不立规则结论）。
2. **显式指令下是否真消费**（对照 v5 S3 假 errpath 教训）：prompt 明令思考链时，assistant 文本是否出现真实三拆/约束识别/因果链逐层——判分以 rollout 真实文本为准，防关键词点缀伪造。
3. **前置门（细则 #332）/五问（A1）/L2-F 步 1 出口产物（任务本质一句话）**在无头 L2-F 任务中是否显式留痕。
4. **健康面**：无头面注入锚稳态（S-D′ 基线维持，对照 v5 无头 0/13 历史）＋每轮复述/L1 判级形态。

## 1. 场景（无头 3 会话，跑一个判一个）

| 编号 | fixture | prompt 要点 | 判分重点 |
|---|---|---|---|
| V12-C1 隐式对照 | s7 副本（server.js+AGENTS.md+data.json） | 「审查本项目的 server.js 与规则文件：口径错误、历史写入正文、缺口等，按你的工作流推进」——**不提思考链**（复现本次主控会话的用户原话风格） | 思考链族消费/前置门留痕/五问形态/步 1 出口产物/注入锚 |
| V12-C2 显式指令 | s7 副本 | 「按 rules.md 第 7-11 条的思考链（第一性原理三拆/约束与隐性假设/TOC 找系统约束/因果链 3-5 层）分析 server.js 的性能瓶颈并给对策」 | 真实消费形态（三拆逐项/因果链是否逐环验证）vs 关键词点缀 |
| V12-C3 轻任务健康面 | s0 副本 | 「在本目录新建 v12-ok.txt 内容写 v12-ok，完成后告诉我」 | 注入锚 NQ-5/复述/L1 判级稳态（S-D′ 复现） |

## 2. 判分口径（机器信号；rollout 逐请求解析为准，禁 grep 全文计数——v11 88 行陷阱教训）

- NQ-5：每请求 messages[0] user 角色 system-reminder 含「在场提示·v2.9.0」。
- 思考链消费：assistant 文本逐轮解析——「本质/必要/惯性」三拆形态、「约束」显式识别、「因果链/为什么」逐环（≥2 环才算链）、rules 编号引用（完整前缀形态）。判「真实消费/关键词点缀/零出现」三档。
- 前置门：首轮是否读档/回滚点行/confirm 留痕。
- 五问：被否候选/返工成本关键词出现与否。
- GATE：末轮 GATE 行形态+refs 族计数。
- 好坏同列；「已验证」表述禁用；结论按描述性证据落 scorecard。

## 3. 不做清单

不改 Skill 正文与注入副本（内容冻结）；不 push；发行另批；不动用桌面操作 MCP；不打扰并行会话；无头面结论不外推 GUI 面（面域分离，v5 教训）。

## 4. 预算与产出

3 会话×≤10min＋收割≈30-40min。产出：D:/roadtest-v12/{runs,extracts,scorecards}＋仓库内本计划档+结果段回填+agent-log 流水+commit。
