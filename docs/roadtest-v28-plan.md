# 路测 v11 计划 · v2.8.0 新条款行为面验证（预注册 · 待执行）

> 编制：2026-09-14 08:40（sess_3367d1f4，9:00 时间盒内落档）。模型锁定 **GLM-5.3-Flash**；token 无限制；执行须整块时间窗（预算 3-4h：冒烟已完，主战场缩编），**另会话/另窗执行**。
> 基线：v2.8.0/330 条（commit 39cc21a+c450950 之后），五副本重部署 ✓。冒烟 sess_2eeb21fc 已单点验证三机制在场。
> **执行状态（2026-09-15 追加）**：v11 部分执行——S-A′ 四轮+手动压缩×2（注入版本分裂机制定论）、S-C′ 三会话、V11 冒烟、受控实验三组（压缩失真/Bash 失败守卫/注入分裂）、并行舰队自查四份 face G n=21（EVIDENCE §二十九追记二/四 + v28-final-call §一/§四）；全量续跑（S-B′ MySQL 环境、S-A′ 余轮、审查会话 GUI 或安静后补跑）入 2.9 议程。

## 0. 本轮核心问题（v10 收敛出的缺口，2.7.2/2.8.0 新条款的效力验证）

1. **每轮复述强制（#325）**：追加/继续/追问轮是否真出「一行复述（收到 X｜理解为 Y｜边界 Z）」？（v10 中段 0 复述、博客链 29 轮 2 状态行的直接对治）
2. **回指加载强制（ANCHOR+hooks 双通道）**：会话是否主动用 Skill 工具加载 SKILL.md？（三样本 0 加载/仅明令 1 次的对治）
3. **压缩接续+重载（#326 扩）**：/compact 后首轮是否=接续声明+验收复述+**重载在用 Skills**？（受控实验实锤压缩后 Skill 全文失真）
4. **开工前置强制门（#332）**：调研基准/在用 Skill 加载/MCP 盘点/回滚点四项是否前置于主流程且豁免留痕？
5. **UserPromptSubmit 每轮再触达**：机制已证在场（冒烟 1 次）；验证行为效力=长会话晚段纪律是否无衰减（对照 v10 S-A C3 后状态行归零）。
6. **Bash 失败守卫（post_tool_guard）**：Bash 非零退出是否真收 TOP 推送并改变处置行为（HKEXP2 单点已证到达）。

## 1. 场景（v10 五场景缩编复用）

| 编号 | 场景 | 轮次 | 新增判据 |
|---|---|---|---|
| S-A' | 长会话真实项目（GUI，Flash） | 15-20 轮+手动压缩×2 | 上 6 项全测；压缩前后对比段 |
| S-B' | 接手遗留项目（ferry 库存前端/连库实测，补 v10 exempt） | 30-40min | 前置门 #332+MySQL 环境准备 |
| S-C' | 无头回归（S0/S3/S7 型） | 3 会话 | NQ-5 稳态+新条款在无头面表现 |
| S-D' | 轻任务单发 | 1 | 每轮复述+L1 判级 |
| S-E' | 自查对照 ×2 | — | face G n=19 |

变体池沿用 plan-frozen-v9 + v10 新增（#317 分诊/压缩测针）；机证=extract_v10+se_verify+hooks-log（新增 UserPromptSubmit 计数列）。

## 2. 不做清单

2.7/2.8 内容冻结不当场改文；无头不得替代 GUI 主战场；本仓不 push；发行另批；不打扰并行会话（双 CUA 暂停协议沿用）。

## 3. 时间预算

S-A' 90-120min+S-B' 40min+S-C' 15min+S-D' 5min+S-E' 20min+收割 40min+收尾 30min ≈ **4-4.5h**；建议整块窗（9:00 后任意时段，与主控会话约定后执行）。
