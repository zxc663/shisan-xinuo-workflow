# 角色包映射表（批次③ 前置呈审稿 · 2026-09-18）

> 状态：**呈审中**——按用户第四轮定规「映射表先过目→再定稿 8 角色正文」。本稿=批次③ 定稿依据；确认（或批注修改）后动工建 `shisan-xinuo-roles` 包。
> 依据：`docs/skill-split-research-20260918.md` §6（角色六字段解剖学+VoltAgent 分类学对照）｜`docs/skill-split-plan-v3.0.md` §一（批③ 范围）｜details 现行条目（引用编号，**不复制内容**——防棘轮：角色是视图，细则单一源）。

## 一、六字段解剖学（每个角色文件的统一骨架）

| 字段 | 内容 |
|---|---|
| ①身份与视角 | 这个角色以什么立场看问题（一句话身份句） |
| ②适用场景/触发 | 什么时候该派它（症状/阶段） |
| ③所要求的流程 | **按什么顺序做事**（用户定义的核心字段） |
| ④审查/执行清单 | 查什么维度——**引用 details 编号/规则号，不复制正文** |
| ⑤出口契约 | 输出什么格式（发现/风险/结论结构） |
| ⑥能力边界 | 能做什么、不能做什么（只读/最小修复/测量；平台无 tools 白名单→prompt 声明降级） |

## 二、8 角色映射主表

| # | 角色 | 身份一句话 | 场景（触发） | 清单来源（引用） | 出口契约 | 复用 |
|---|---|---|---|---|---|---|
| 1 | **critic** | 对抗式方案/设计批评者 | 第 7 步规划产出后 / 第 9 步提交前 / 用户点名评审 | 现有 7 项 + `#351`（影响矩阵完整性）+ 规则 4/5（复用铁律）+ Y1（YAGNI） | 发现[严重度]/待澄清/残余风险/结论 | **现有模板改造** |
| 2 | **risk-reviewer** | 技术风险评审者（双模式） | 规划期（第 6-7 步）/ 提交前（第 9 步） | 现有 7 维 + `#307`/`#343`（版本控制与提交粒度）+ `#337`（迁移工具）+ `#328`（系统级配置）+ `#335`（性能预算入验收） | 风险摘要（可能性×影响+缓解）/最终评审（N/A 显式） | **现有模板改造** |
| 3 | **security-auditor** | 安全审计者 | 密钥/权限/外部输入/依赖/发布（L3 类）或公开推送前 | 现有 7 项 + security §1.7 应用安全基线 + NEVER list + `#334`（依赖与锁文件）+ 类 22（安全会话令牌） | 发现+严重度 P0-P3+建议处置+事件记录需求 | **现有模板改造** |
| 4 | **debugger** | 系统化排错者（铁律：先根因后修复） | 报错/异常/行为不符/unexpected——**动手修之前** | 错误必查 TOP 八条（`#294`/`#233`/`#228`/`#229`/`#214`/`#163`/`#256·#269`/`#262`）+ `#255`（四要件）+ `#227`（规格先呈报）+ `#16`/`#147`/`#154`（异步/并发类） | 根因一句+errpath 取证行+修复+验证证据 | 新增（Iron Law 文风参照 superpowers） |
| 5 | **contract** | 对接与契约审查者 | 新端点/跨包对接/schema/序列化/中间层改动 | `#351`（影响矩阵）+ `#233`/`#227`（命名直觉/规格）+ `#214`/`#163`/`#269`/`#262`（契约族）+ `#338`（正向设计）+ `#337`（库变更） | 真相/影响矩阵+分层断言清单 | 新增 |
| 6 | **test** | 测试策略与防假绿审查者 | 测试策略/边界覆盖/「跑通了」验证/验证降配 | `#339`（策略显式化）+ `#266`（双负载形状）+ `#255`（四要件）+ `#345`（收敛判据）+ `#354`（完成六件套）+ `#251`（视口外断言） | 边界用例清单（≥3）+证据判读+降配声明检查 | 新增 |
| 7 | **frontend** | 前端体验审查者 | UI/交互/样式/动效/图表类任务的设计与审查 | `#286`（令牌零魔数）+ `#288`（动效三查）+ `#289`（可视化基线）+ `#292`（冗余容错）+ `#248`（视口截图+DOM 佐证）+ `#284`/`#312`（设计规范档先行） | 设计档回指+走查清单（DOM 佐证） | 新增 |
| 8 | **perf** | 性能审查者（测量先于优化） | 性能预算制定/性能问题定位/并发卡顿 | `#335`（预算三行）+ `#193`（反馈落交互点）+ `#266`（保序 vs 吞吐）+ `#147`（分批并发防竞态）+ `#154`（并发数压垮服务） | 预算表+测量证据+优化前后对照 | 新增 |

## 三、dispatch 矩阵（症状/场景 → 角色，写入 roles 包 SKILL.md 与核心路由表）

| 场景/症状 | 角色 |
|---|---|
| 方案/设计/规划评审 | critic |
| 提交/上线/迁移/破坏性变更前 | risk-reviewer |
| 密钥/权限/外部输入/依赖/发布（L3 类） | security-auditor |
| 报错/异常/行为不符（动手修之前） | debugger |
| 新端点/跨包对接/schema/序列化/中间层 | contract |
| 测试策略/边界覆盖/「跑通了」/验证降配 | test |
| UI/交互/样式/动效/图表 | frontend |
| 性能/耗时/并发卡顿/预算 | perf |

## 四、批③ 施工范围（映射表确认后执行）

1. 建 `skill/shisan-xinuo-roles/`：`SKILL.md`（description 触发面+**依赖声明**+加载后行动契约+dispatch 矩阵）+ `roles/` 8 文件（`critic.md`/`risk-reviewer.md`/`security-auditor.md`/`debugger.md`/`contract.md`/`test.md`/`frontend.md`/`perf.md`，中文正文、英文 kebab 文件名）。
2. **三模板迁移改造**：`git mv` 核心 `templates/agents/` 三文件 → `shisan-xinuo-roles/roles/`，按六字段改造（保留身份句+评审清单，补③流程/⑥能力边界/清单编号引用）。
3. 核心同步：SKILL 路由表角色行指向 roles 包实体；§13/尾注更新；README 树+project-info 模块表加 roles 行；plan §五③✅。
4. 验收：verify 7/7+残留引用扫描；触发探针归收尾部署批（exempt）。

## 五、呈审四问（请逐条确认或批注）

1. **8 角色构成与清单映射**：有无增删/错配（尤其 debugger 的 TOP 八条、contract 的契约族六条是否就是你要的维度）？
2. **能力边界的降级形态**：ZCode 子代理无 tools 白名单机制——⑥ 以 prompt 声明（如「只读，不得写文件」）承担，接受此降级？
3. **dispatch 触发词**：上表 8 行是否够准（要增触发词/合并角色/加优先级规则说一声）？
4. **文件命名与位置**：`shisan-xinuo-roles/roles/<英文-kebab>.md`（中文显示名在正文首行），三模板 git mv 迁入——OK？
