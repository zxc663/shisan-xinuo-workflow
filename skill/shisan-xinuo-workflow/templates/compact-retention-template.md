# /compact 保留指令模板（细则 #326 配套 · 手动压缩时粘贴在 /compact 后）

> 用法：`/compact <以下四段按需填>`——压缩摘要只保留这里点名的内容，未点名的高价值状态会被泛化摘要稀释。

保留：任务本质=<一句话：这个会话在做什么/交付什么>；已交付=<版本/commit/关键产物路径>；验收标准=<逐条列出，压缩后凭此对齐理解>；当前波次=<进行到哪一步/下一步第一动作>；回滚基线=<commit/备份位置>；**在用 Skills=<本会话已加载的 Skill 清单，压缩后须逐个重载>**；设计档与关键决策=<docs/design-specs/ 路径或决策一行>；<其他不可丢的具体状态：账号/端口/环境变量名等>
